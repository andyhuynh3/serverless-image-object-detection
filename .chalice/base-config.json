{
  "version": "2.0",
  "app_name": "serverless-object-detection",
  "stages": {
    "dev": {
      "api_gateway_stage": "api",
      "autogen_policy": false,
      "environment_variables": {
        "IMAGE_INPUT_BUCKET": "%s",
        "IMAGE_OUTPUT_BUCKET": "%s",
        "DYNAMODB_TABLE": "%s"
      }
    },
    "prod": {
      "api_gateway_stage": "api",
      "autogen_policy": false,
      "environment_variables": {
        "IMAGE_INPUT_BUCKET": "%s",
        "IMAGE_OUTPUT_BUCKET": "%s",
        "DYNAMODB_TABLE": "%s"
      }
    }
  }
}
